{
  "buildCommand": "cd \"front harryPotter\" && npm install && npm run build",
  "outputDirectory": "front harryPotter/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
